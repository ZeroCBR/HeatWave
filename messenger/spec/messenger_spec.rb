require 'spec_helper'

describe Messenger do
  it 'has a version number' do
    expect(Messenger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
